#!/usr/bin/env sh
cd "$(dirname -- "$0")/.." || exit 1
. "$(dirname -- "$0")/ensure-bun.sh"

echo "[pre-push] rebuilding changelog"
bun run changelog:husky
echo "[pre-push] running repo checks"
bun run check-pre-push
